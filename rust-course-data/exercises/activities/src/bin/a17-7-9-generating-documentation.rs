// cargo doc --open

#![allow(dead_code)]
/// a favorite color
enum Color {
    Red,
    Blue,
}
/// a piece of mail.
struct Mail {
    /// the destination address
    address: String,
}

/// adds two numbers together
fn add(a: i32, b: i32) -> i32 {
    a + b 
}

fn main() {

}
