fn main() {
    let names = [String::from("liming"), String::from("hanmeimei")];
    for name in &names {

    }
    println!("{names:?}");

    let numbers = [1, 2, 3];
    for n in numbers {

    }
    println!("{numbers:?}");
}
